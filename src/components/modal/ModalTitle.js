import styled from '@emotion/styled';

const ModalTitle = styled.div`
  font-size: 2.6rem;
  font-weight: bold;
  line-height: 1.2;
  text-align: center;
  margin: 1rem 0 3rem;
  opacity: 0.9;
`;

export default ModalTitle;
